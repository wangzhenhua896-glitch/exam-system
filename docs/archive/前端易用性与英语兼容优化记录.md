# 前端易用性与英语兼容优化记录

日期：2026-04-12
涉及文件：`dist/index.html`、`templates/question-bank.html`、`templates/test-cases.html`、`static/js/use*.js`、`app/api_routes.py`

---

## 一、英语科目前端兼容（8 项）

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | 字数统计单位硬编码"字" | 英语显示 words，中文显示 字 | `dist/index.html` |
| 2 | 满分单位硬编码"分" | 英语 pts，中文 分 | `dist/index.html` |
| 3 | 手动输入 subject fallback 为 general | 新增 `manualSubject` ref + 科目选择器 | `useQuestion.js` / `useApp.js` / `useGrading.js` / `dist/index.html` |
| 4 | `genStyles` 默认中文值 + watch 无 immediate | 默认空数组，watch 加 `{ immediate: true }` | `useTestSet.js` |
| 5 | `toLocaleString('zh-CN')` 强制中文日期 | 改为 `toISOString()` | `useTestSet.js` |
| 6 | 详情页 raw subject 输出 | 用 `getSubjectLabel()` 替换 | `test-cases.html` |
| 7 | 评分脚本解析正则缺 Part/Section | 增加 `Part\s*\d+` / `Section\s*[A-Z]` | `useRubric.js` |
| 8 | 反作弊缺英语特有作弊模式 | 新增"中文翻译作答"场景（仅英语），非英语自动跳过 | `useAntiCheat.js` |

## 二、P0 易用性修复（3 项）

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | 术语不统一（评分约定 vs 评分脚本） | 全部统一为"评分脚本" | `question-bank.html` |
| 2 | 回滚无确认 | `rollbackScriptVersion` 加 `ElMessageBox.confirm` | `useRubric.js` |
| 3 | 清空历史无确认 | `clearHistory` 加 `ElMessageBox.confirm` | `useHistory.js` |
| 4 | 手动输入要求 JSON 格式 | 改为表单化得分点编辑器（描述+分值，可增删），自动构建 rubric | `dist/index.html` / `useGrading.js` / `useQuestion.js` / `useApp.js` |

## 三、导入导出扩展（2 项）

### 3.1 Word 导入

| 文件 | 改动 |
|------|------|
| `app/api_routes.py` | `/import-questions/preview` 和 `/import-questions` 增加 `.docx` 支持，用 `python-docx` 解析 Word 表格为 DataFrame，支持合并单元格补齐 |
| `question-bank.html` | upload accept 增加 `.docx`，按钮文案"导入Excel"→"导入文件" |
| `requirements.txt` | 增加 `python-docx==1.2.0` |

### 3.2 Word 导出

| 文件 | 改动 |
|------|------|
| `app/api_routes.py` | `/export-rubric-scripts` 增加 `format=docx` 参数，用 `python-docx` 生成 Word（H0/H1/H2 标题层级、题目、标准答案、评分脚本段落） |
| `question-bank.html` | 导出按钮改为下拉菜单（Markdown / Word），`exportRubricScripts(fmt)` 接收格式参数 |

## 四、P1 易用性修复（5 项）

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | 导航名含歧义 | "单题处理"→"出题"，"规则完善"→"测试评分" | `question-bank.html` |
| 2 | 三层并行评分暴露 | 改为 `el-collapse` 折叠面板，默认收起 | `dist/index.html` |
| 3 | 引擎/记录ID 暴露 | `display: none` 隐藏 | `dist/index.html` |
| 4 | 模型选择下拉框暴露 | `display: none` 隐藏 | `dist/index.html` |
| 5 | 评分策略暴露 | `display: none` 隐藏 | `question-bank.html` |
| 6 | 手动输入无引导 | 未选题时显示蓝色引导框"请先从上方下拉框选择一道题目" | `dist/index.html` |
| 7 | 质量检查入口太小 | 12px 灰字链接 → 独立按钮（一致检查=橙色，命题质量=绿/红随分数变色） | `question-bank.html` |

## 五、P2 易用性修复（4 项）

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | 测试用例运行后无对比视图 | 增加"详情"按钮 + 评分详情对话框（期望分/实际分/答案/评分明细/评语） | `dist/index.html` / `useTestSet.js` / `useApp.js` |
| 2 | 评分脚本无模板引导 | 编辑模式显示格式提示（5段结构说明） | `dist/index.html` |
| 3 | 创建子题入口隐蔽 | 父题操作栏增加"添加子题"按钮 | `question-bank.html` |
| 4 | 测试用例类型标签不一致 | 统一为三种：真实/AI生成/人工模拟 | `dist/index.html` |

## 六、P3 易用性修复（1 项）

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | 父题满分不一致无自动修正 | 展开行中满分不一致时显示"自动修正满分"按钮，点击后将父题满分改为子题之和 | `question-bank.html` |

## 七、P4 易用性修复（4 项）

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | 生成数量默认 7 个无解释 | 加"推荐 7-10 个以覆盖各分数段"说明 | `dist/index.html` |
| 2 | 分数分布策略太技术化 | 改文案为"各水平均覆盖/重点测极端情况/重点测中等水平/均匀分布" | `dist/index.html` |
| 3 | 底部导航暴露过多链接 | 精简为"题库管理/聚焦评分/测试集"，去掉"敏感词/用户管理" | `question-bank.html` |
| 4 | 自查 category 标签不明 | 做友好映射（逻辑歧义→逻辑问题，分数分配→分数问题等） | `question-bank.html` |

---

## 未处理问题（P2 中跳过的）

| # | 问题 | 原因 |
|---|------|------|
| 1 | 生成入口分散（测试集 vs 验证约定） | 两个页面服务不同工作流，强行统一会增加复杂度 |
| 2 | 子题列表重复显示 | 经确认 `filteredQuestions` 已过滤 `!q.parent_id`，子题只在展开行显示 |
| 3 | 两套 SPA 状态不共享 | 结构性问题，需较大重构 |
| 4 | 题目选择下拉框无分页 | 当前题库规模不大，后续再处理 |

## 涉及文件清单

```
dist/index.html                          # SPA 单题评分页（8 处改动）
templates/question-bank.html             # 题库管理页（12 处改动）
templates/test-cases.html                # 测试集管理页（1 处改动）
static/js/useQuestion.js                 # 题目模块（新增 manualSubject + manualRubricPoints）
static/js/useGrading.js                  # 评分模块（错误提示分级 + rubric 构建逻辑）
static/js/useTestSet.js                  # 测试集模块（genStyles immediate + testCaseDetail + loadedTestCaseInfo）
static/js/useRubric.js                   # 评分脚本模块（正则扩展 + 回滚确认）
static/js/useAntiCheat.js                # 反作弊模块（新增中文翻译作答场景）
static/js/useHistory.js                  # 历史模块（清空确认）
static/js/useApp.js                      # 整合模块（字号范围 + 新 ref 贯穿）
app/api_routes.py                        # 后端 API（Word 导入导出）
requirements.txt                         # 依赖（python-docx）
```
