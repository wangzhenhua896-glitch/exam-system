# 命题质量检查弹窗优化记录

日期：2026-04-11

## 1. 文案替换："科学性" → "命题质量检查"

文件：`templates/question-bank.html`，共 4 处

| 位置 | 原文 | 改后 |
|------|------|------|
| 第303行（按钮文案） | `科学性: X分` / `科学性评估` | `命题质量检查: X分` / `命题质量评估` |
| 第1908行（弹窗标题） | `试题科学性评估报告` | `命题质量检查报告` |
| 第1917行（错误提示） | `科学性评估失败` | `命题质量评估失败` |
| 第1929行（保存拦截提示） | `试题科学性评估未通过` | `命题质量检查未通过` |

## 2. 报告弹窗优化

文件：`templates/question-bank.html`，`evaluateQuality` 函数

### 改动内容

- **弹窗宽度**：560px → 1120px（加一倍）
- **整体字号加一倍**：
  - 分数：48px → 96px
  - 判标：16px → 32px
  - 问题标题：12px → 24px
  - 问题描述：13px → 26px
  - 建议列表：13px → 26px
- **新增"保存分数"按钮**：
  - 关闭按钮文案改为"关闭"
  - 新增"保存分数"按钮（取消按钮位置），点击后调用 `saveQuestion()` 保存当前题目（含 qualityScore）

### 效果

用户评估命题质量后，可直接在报告弹窗中点击"保存分数"将结果持久化，无需关闭弹窗后再手动保存。
