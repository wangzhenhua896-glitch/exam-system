# 前端代码审查与改进清单

> 审查时间：2026-04-14
> 最后更新：2026-04-15（前端 8 轮拆分完成后）
> 审查范围：全部前端代码（16 个模板 + dist/index.html + 12 个 JS 文件 + 2 个 CSS 文件）

---

## 统计数据

| 类别 | 文件数 | 总行数 | 总字节 |
|------|--------|--------|--------|
| HTML 模板 | 16 | ~6,800 | — |
| JS（自研） | 12 | ~3,200 | — |
| CSS（自研） | 2 | ~870 | — |
| dist/index.html | 1 | ~856 | 74 KB |
| Vendor（全部） | 9 | 104 | 4.8 MB |
| Vendor（无引用） | 3 | 11 | 3.2 MB |

---

## P0 — 必须修复（影响功能正确性或用户体验）

### P0-1：评分脚本编辑器滚轮劫持 ✅ 已修复

- **文件**：question-edit.html（rubricScript watcher）、rubric-workbench.html
- **问题**：watcher 监听 `questionForm.rubricScript`，每次内容变化都执行 `scrollTop = 0`。用户编辑长脚本时每敲一个字页面跳回开头，无法正常使用。
- **根因**：watcher 没有区分"加载新脚本"和"用户编辑"两种场景。
- **修复**：加 `!oldVal && newVal` 守卫，只在内容从空变为非空时重置滚动位置。question-edit.html:364、rubric-workbench.html:272 均已修复。

### P0-2：删除操作吞掉所有错误 ✅ 已修复

- **文件**：dedup.html（confirmMerge, confirmExactMerge）、question-edit.html（parseScoringPointJson）
- **问题**：catch 块用 `/* cancelled */` 把所有异常（网络错误、服务器 500）全部静默吞掉。
- **修复**：dedup.html 两处 catch 改为区分 `cancel` 和真正异常；question-edit.html 加 `console.warn`。

### P0-3：多处加载失败无用户提示 ✅ 已修复

- **文件**：login.html、englishEditCore.js、test-cases.html、useHistory.js
- **问题**：数据加载/保存失败时只打 console，用户无感知。
- **修复**：4 处 catch 块分别加 `ElMessage.warning`/`ElMessage.info`/`console.warn` 提示。

### P0-4：JSON.parse 无保护导致白屏 ✅ 已修复

- **文件**：dashboard.html、question-view.html、englishEditCore.js
- **问题**：`JSON.parse(localStorage.getItem(...))` 无 try-catch，localStorage 被污染时页面白屏。
- **修复**：shared.js 新增 `safeJsonParse()` 和 `loadSubjectsFromStorage()`，dashboard.html 和 question-view.html 替换为安全调用；englishEditCore.js 的 workflow_status 解析加独立 try-catch。

### P0-5：英语编辑器阅读材料丢失格式 ✅ 已修复

- **文件**：englishEditCore.js
- **问题**：`parentContentHtml` 依赖 `original_text`/`content_html`，两字段为空时回退到 textarea，HTML 格式丢失。
- **修复**：当 `original_text` 和 `content_html` 都为空时，回退使用含 HTML 标签的 `content` 字段。

---

## P1 — 应该修复（代码质量和可维护性）

### P1-1：3.2 MB 无用 vendor 文件

- **文件**：`static/vendor/tailwind.min.css`(2.9MB)、`quill.js`(209KB)、`quill.snow.css`(24KB)
- **问题**：这三个文件没有被任何页面引用，白占磁盘空间和部署传输时间。
- **建议**：删除。

### P1-2：style.css 是死代码

- **文件**：`static/css/style.css`（808 行暗色主题 CSS）
- **问题**：没有被任何页面加载。当前所有页面用的是行内 `<style>` 的亮色主题。
- **建议**：删除，或确认是否计划迁移到暗色主题。如果计划迁移，加注释说明用途和时间表。

### P1-3：question-bank.html 巨型单文件 ~~3581 行~~ → ~898 行 ✅ 已完成

- **文件**：question-bank.html（~898 行，含英语编辑器 UI 壳 ~456 行）
- **状态**：8 轮拆分全部完成，10 个独立页面 + rubric-workbench 共 11 个新页面。剩余：题目列表（核心）、英语编辑器（已模块化）。

### P1-4：20+ 个完全相同的 catch 块 ✅ 已完成

- **状态**：`handleApiError` 已提取到 `static/js/shared.js`，通过 `window.SharedApp.handleApiError` 全局暴露，各页面已接入。

### P1-5：4 个相同的表单校验守卫

- **文件**：question-edit.html（autoGeneratePoints, generateRubric, selfCheckRubric, evaluateQuality）
- **问题**：每个函数都以相同的两行校验开头：
  ```js
  if (!questionForm.content.trim()) { ElMessage.warning('请先填写题干内容'); return; }
  if (!questionForm.standardAnswer.trim()) { ElMessage.warning('请先填写标准答案'); return; }
  ```
- **建议**：提取为 `validateQuestionForm()` 函数。

### P1-6：confirmMerge 和 confirmExactMerge 近乎重复

- **文件**：dedup.html（confirmMerge, confirmExactMerge）
- **问题**：两个函数流程完全一样（获取分组 → 构建删除 ID → 循环确认 → 调用合并 API），唯一区别是一个传 `updateFields` 一个传 `{}`。
- **建议**：合并为一个函数，通过参数控制是否传 `updateFields`。

### P1-7：viewQuestion 和 editQuestion 80% 重复

- **文件**：question-edit.html（viewQuestion, editQuestion）
- **问题**：
  ```js
  // 两个函数都做这些事：
  currentPage.value = 'question-manage';
  viewMode.value = '...'; // 唯一区别
  editingQuestionId.value = questionId;
  sidebarCollapsed.value = true;
  const q = await loadQuestionToForm(questionId);
  if (!q) { resetForm(); }
  ```
- **建议**：合并为 `navigateToQuestion(questionId, mode)`。

### P1-8：getSubjectLabel 变量遮蔽

- **文件**：question-bank.html（从 SharedApp 解构后又本地重新定义）
- **问题**：本地函数覆盖了从 shared.js 导入的同名函数，容易造成困惑。
- **建议**：删除 1804 行的本地定义，统一使用 `window.SharedApp.getSubjectLabel(value, subjectList.value)`。

### P1-9：admin.html 混用 fetch 和 axios

- **文件**：admin.html（7 个 `fetch()` 调用）
- **问题**：admin.html 是唯一用原生 `fetch()` 调 API 的页面，其他所有页面都用 axios。没有统一的错误处理和拦截器。
- **建议**：统一改为 axios。

### P1-10：autoParseChinese 函数 127 行，深度嵌套

- **文件**：question-edit.html（autoParseChinese）
- **问题**：正则匹配、段落追踪、启发式内容提取全部混在一个函数里，难以理解和维护。
- **建议**：拆分为 `extractSections()`、`parseScore()`、`extractPoints()`、`extractRules()` 等子函数。

### P1-11：autoParseEnglish 和 autoParseChinese 结构重复

- **文件**：question-edit.html（autoParseEnglish, autoParseChinese）
- **问题**：两个函数的高层流程完全一致（初始化 → 解析分值 → 分割段落 → 提取得分点 → 填充表单 → 显示结果），只有正则和解析规则不同。
- **建议**：提取通用框架函数 `autoParse(text, config)`，中英文各提供不同的 config。

### P1-12：考试大纲和教材内容页面近乎完全一样

- **文件**：syllabus.html（两个 tab 的表单结构完全相同）
- **问题**：两个页面结构完全相同（科目选择器 + 标题输入 + 内容文本域 + 保存/清除按钮 + 上次保存时间），只是 `type` 参数不同。
- **建议**：合并为一个组件，通过 prop 区分类型。

### P1-13：4 个相同的标签输入模式

- **文件**：question-edit.html / englishEditCore.js（关键词、同义词、排除词、拼音白名单）
- **问题**：
  ```html
  <div style="display: flex; flex-wrap: wrap; gap: 6px; ...">
      <el-tag v-for="..." closable size="small" @close="...">{{ ... }}</el-tag>
      <el-input size="small" style="width: 180px;" ... />
  </div>
  ```
  完全相同的模式出现 4 次。
- **建议**：提取为 `<tag-input>` 组件（或 composable），接受 `list`、`placeholder`、`onAdd`、`onRemove` 等 props。

---

## P2 — 建议优化（体验和性能）

### P2-1：搜索框无防抖

- **文件**：question-bank.html（searchKeyword + filteredQuestions computed）
- **问题**：`searchKeyword` 每次按键都触发 `filteredQuestions` 重新过滤 + 排序 + 渲染整个表格。`sort()` 每次创建新数组，导致表格完整重绘。
- **建议**：搜索框加 300ms debounce。

### P2-2：CSS reset 和 Element Plus 字体覆盖应移入 shared.css

- **文件**：question-bank.html（行内 style 块）
- **问题**：
  ```css
  * { margin: 0; padding: 0; box-sizing: border-box; }
  .el-table { font-size: 14px; }
  .el-button { font-size: 14px; }
  /* 等等 */
  ```
  这些全局覆盖在 question-bank.html 行内定义，其他页面没有享受到，导致样式不一致。
- **建议**：移入 shared.css，所有页面统一。

### P2-3：大量行内 style 应提取为 CSS 类

- **文件**：question-bank.html 全文
- **问题**：`padding:6px 10px; background:#f5f7fa; border-radius:4px` 这类模式出现几十次。`style="display: flex; align-items: center; justify-content: space-between;"` 也重复多次。
- **建议**：定义工具类如 `.info-block`、`.flex-between`、`.text-center` 等。

### P2-4：去重对话框中 3 个近乎相同的表格

- **文件**：dedup.html（合并模式/精确去重/相似去重）
- **问题**：三个表格的列定义（ID/题号/题干/分值/质量/脚本状态/操作）几乎完全一样。
- **建议**：提取为一个可复用的表格组件或模板片段。

### P2-5：quality score 标签重复 4 次

- **文件**：question-bank.html 多处
- **问题**：
  ```html
  <el-tag :type="score >= 80 ? 'success' : score >= 60 ? 'warning' : 'danger'">
  ```
  同一套 >=80/>=60/<60 判断逻辑出现 4 次。
- **建议**：提取为 `<quality-badge :score="..." />` 组件。

### P2-6：useQuestion.js 重复定义 SUBJECT_LABELS

- **文件**：useQuestion.js:94
- **问题**：shared.js 已有完整的 10 科目映射，useQuestion.js 又定义了一个简化的 4 科目版本。
- **建议**：统一使用 `window.SharedApp.SUBJECT_LABELS`。

### P2-7：skipExactGroup 和 skipGroup 完全相同

- **文件**：question-bank.html（skipExactGroup:3138, skipGroup:3142）
- **问题**：两个函数体都是 `advanceToNext()`，完全冗余。
- **建议**：删除其中一个。

### P2-8：ElMessageBox cancel 判断不严谨

- **文件**：question-bank.html 多处
- **问题**：用 `e !== 'cancel'` 判断用户是否取消，但 ElMessageBox 也可能抛 `'close'` 等其他非 Error 值。
- **建议**：统一用 `e instanceof Error` 判断是真正的异常还是用户操作取消。

### P2-9：currentSubjectLabel 重复调用 JSON.parse

- **文件**：question-bank.html（currentSubjectLabel computed）
- **问题**：computed 属性每次求值都执行 `JSON.parse(localStorage.getItem(...))`。localStorage 是同步 I/O，JSON.parse 也有开销。
- **建议**：将 subject 列表缓存到 ref 中，computed 只做数组查找。

### P2-10：evaluateQuality 构建 HTML 字符串有 XSS 风险

- **文件**：question-edit.html（evaluateQuality）
- **问题**：通过字符串拼接构建 HTML，并用 `dangerouslyUseHTMLString: true` 传给 ElMessageBox。如果 `issue.description` 包含用户控制内容，存在 XSS 风险。
- **建议**：对用户输入内容做 HTML 转义后再拼接，或改用 Vue 组件渲染报告内容。

---

## 已知死代码清单

| 位置 | 内容 | 状态 |
|------|------|------|
| question-bank.html | 注释掉的「评分工具」菜单 | 应删除 |
| question-edit.html | `display: none` 隐藏的「基本信息」section | 应删除或恢复 |
| question-edit.html | 双重 `display: none` 的评分策略选择器 | 应删除 |
| static/css/style.css | 808 行暗色主题，无页面引用 | 应删除 |
| static/vendor/tailwind.min.css | 2.9 MB，无页面引用 | 应删除 |
| static/vendor/quill.js + quill.snow.css | 234 KB，无页面引用 | 应删除 |

---

## 架构层面问题

### 两套模块系统并存

当前前端同时存在两种模块模式：
1. **IIFE 全局变量**（window.SharedApp, window.EnglishEditCore）：shared.js、englishEditCore.js 和所有模板页面的行内 script
2. **ES Modules**（import/export）：dist/index.html 使用的 use*.js 模块

两套系统无法互相引用。ES Module 中无法直接用 `window.SharedApp`（虽然技术上可以但不规范），IIFE 中无法用 import。长期来看应该统一。

### 5 个模板页面没有使用 composable 架构

login.html、admin.html、test-cases.html、sensitive-words.html、user-management.html、question-bank.html、question-edit.html、dedup.html、import.html、ai-generate.html、syllabus.html 的 Vue 逻辑都是行内 `<script>`，使用 IIFE 全局变量模式。随着功能增长，这些页面也会面临巨型单文件问题。

### dist/index.html 和 question-bank.html 大量重复

dist/index.html 是评分功能的独立页面（~856 行），与 question-edit.html 的评分脚本生成/验证逻辑有重叠。应该确认 dist/index.html 的定位——如果是独立部署用，考虑通过构建工具自动生成而不是手动同步。
