# 下一步工作计划

> 更新时间：2026-04-15（代码审查后全面修正）
> 基于：3 个 Explore Agent 的全面代码审查结果

---

## 当前状态

- 前端拆分 8 轮完成（3653 → 898 行），11 个独立页面
- 5 个 P0 前端 Bug 全部修复
- async 伪异步清理已完成（主流程），残留 import 待清理
- Vue 3 click 跳转修复、ESLint warning 修复已完成

---

## 修正说明

本次更新对原有计划做了大幅修正，原因：
1. **多个计划项信息过时** — vendor 文件实际在用、端口已统一、校验守卫函数已移除
2. **发现新的 P0 功能缺陷** — 批量评分绕过三层评分、缺少超时机制
3. **优先级重新排序** — 先修 Bug、再补功能、后做重构

---

## 第一轮：修复实际 Bug（立刻）

| 编号 | 事项 | 详情 |
|------|------|------|
| Bug-1 | `autoGeneratePoints` 悬空引用 | question-edit.html 第 229 行引用了不存在的函数，点击即报错 |
| Bug-2 | admin.html fetch 不检查 HTTP 状态码 | 7 处 fetch 不检查 resp.ok，4xx/5xx 静默失败 |

---

## 第二轮：清理确认的死代码（立刻）

| 编号 | 事项 | 详情 |
|------|------|------|
| 清理-1 | 删除 5 个死代码文件 | validation_routes.py, tuning_routes.py, validation.py, auto_tuning.py, test_data.py — 未被 app.py 注册，形成封闭死代码链 |
| 清理-2 | 删除 style.css | static/css/style.css（809 行），零引用 |
| 清理-3 | 移除 set_provider() 残留 | qwen_engine.py 第 52-59 行，竞态修复后残留的死方法 |
| ~~清理-4~~ | ~~删除无用 vendor 文件~~ | **已过时** — tailwind.min.css 不存在；quill.js/quill.snow.css 被两个页面引用，不可删除 |
| ~~清理-5~~ | ~~端口统一到 5001~~ | **已过时** — 三处已是 5001 |
| ~~P1-5~~ | ~~表单校验守卫去重~~ | **已过时** — 函数已移除 |

---

## 第三轮：修复 P0 功能缺陷（短期）

| 编号 | 事项 | 详情 |
|------|------|------|
| P0-1 | 批量评分改走三层评分 | create_batch() 直接调用 grading_engine.grade()，跳过关键词匹配、向量匹配、语义校验、假引用检测。需改用 three_layer_grade() |
| P0-2 | LLM API 调用加 timeout | 当前无超时，API 挂起可导致请求永久阻塞。OpenAI 客户端需配置 timeout 参数 |

---

## 第四轮：统一 admin.html HTTP 客户端（短期）

| 编号 | 事项 | 详情 |
|------|------|------|
| P1-1 | admin.html fetch → axios | 7 处 fetch 统一改为 axios，自动抛 HTTP 错误，与其余页面一致 |

---

## 第五轮：建立测试保护（中期）

| 编号 | 事项 | 详情 |
|------|------|------|
| 测试-1 | _parse_output 单元测试 | qwen_engine.py 的 JSON 解析逻辑，覆盖正常/异常/多匹配/无匹配 |
| 测试-2 | english_scoring_point_match 单元测试 | 英语采分点匹配核心函数，覆盖命中/未命中/拼音/排除词 |
| 测试-3 | _apply_strategy 单元测试 | 三层评分策略取分逻辑（max/min/avg/median） |
| 测试-4 | 批量评分集成测试 | 验证 /api/batch 走三层评分、结果与单题一致 |

> **原则：没有测试保护不动重构**

---

## 第六轮：代码重构（长期，需先完成第五轮）

| 编号 | 事项 | 详情 |
|------|------|------|
| P1-8 | getSubjectLabel 适配模式优化 | 有意的适配模式，风险低，可选做 |
| P1-9 | ~~admin.html fetch→axios~~ | 已并入第四轮 |
| P1-12 | 大纲/教材页面合并 | 收益仅 ~30 行，优先级最低 |
| 重构-1 | 6 处裸 except 改为 Exception | 防止吞掉 KeyboardInterrupt/SystemExit |
| 重构-2 | 残留 import asyncio 清理 | 11 个文件残留 |
| 重构-3 | grade_english_fallback 边界检查对齐 | 与 qwen_engine.boundary_check() 逻辑不一致 |

---

## 不做

| 事项 | 原因 |
|------|------|
| 删除 vendor 文件 | quill.js/quill.snow.css 被活跃引用 |
| 端口统一 | 已经统一 |
| 大纲/教材合并 | 收益太小（~30 行） |
| getSubjectLabel 重构 | 有意的适配模式，风险低 |

---

## ~~旧计划已废弃~~

原文档中的"第三轮：清理技术债"和"第四轮：代码重构"已废弃，请以本文件为准。
