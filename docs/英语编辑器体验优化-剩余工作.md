# 英语编辑器体验优化 — 剩余工作

> 上下文：JS 层 6 项已提交 (commit 4f75aa4)。以下 5 项需改模板 `templates/question-bank.html` 和补充 `englishEditValidateSave.js`。

## 已完成（JS 层，4f75aa4）

| # | 优化项 | 状态 |
|---|--------|------|
| 6 | AI 静默失败 → throw | ✅ |
| 9 | 采分点提取独立 loading | ✅ JS 完成，模板按钮需同步改 `:loading` |
| 1 | 子题返回不重置后续题 | ✅ |
| 10 | 浮动点精度 | ✅ |
| 2 | 保存成功提示 | ✅ |
| 8 | confirm → ElMessageBox | ✅ JS 完成，模板按钮需改调用方式 |

## 待做（模板 + 验证规则）

### 第一批：简单模板同步（2项 JS 已改但模板未跟上）

**P1. 模板同步 `scoringPointExtractingMap`**
- 文件：`templates/question-bank.html` 第339行附近
- 改法：`:loading="englishEdit.extractingLoading"` → `:loading="englishEdit.scoringPointExtractingMap[sq.tempId]"`

**P2. 模板同步 `confirmResetAll`**
- 文件：`templates/question-bank.html` 第190行
- 改法：`@click="confirm('重新提取将清空当前所有采分点和评分脚本配置，确定？') && englishEdit.resetAll()"`
  → `@click="englishEdit.confirmResetAll()"`

### 第二批：新增功能（3项）

**P3. 步骤指示器：锁定步骤视觉提示**
- 文件：`templates/question-bank.html` 第196-218行（步骤指示器区域）
- 改法：
  - 圆圈 div 加 `:style` 增加：`cursor: englishEdit.canEnterStep[step.id] ? 'pointer' : 'not-allowed'` + `opacity: englishEdit.canEnterStep[step.id] ? 1 : 0.5`
  - `@click` 改为调用一个新方法，或者内联：`englishEdit.canEnterStep[step.id] ? englishEdit.goToStep(step.id) : ElMessage.info('请先完成前面的步骤')`
- 注意：模板中可以内联用 `ElementPlus.ElMessage`，或在 core.js 中加一个 `goToStepSafe(stepId)` 方法

**P4. 返回题库按钮（Step5）**
- 文件：`templates/question-bank.html` Step5 保存区域（第627-630行）
- 改法：在保存按钮旁边加 `<el-button @click="currentPage = 'question-list'">返回题库</el-button>`

**P5. 采分点分值小计 + V8 验证规则**
- 模板部分（`templates/question-bank.html` 第335-342行采分点区域）：
  - 在采分点列表标题栏加一行：`采分点分值合计: X/Y ✓✗`
  - X = `sq.scoringPoints.reduce((sum, sp) => sum + (sp.score || 0), 0)`，Y = `sq.maxScore`
- 验证规则部分（`static/js/englishEditValidateSave.js`）：
  - 在 V7 之后新增 V8：每个子题的采分点分值之和必须等于子题满分
  - `state.subQuestions.forEach(function (sq, qi) { var spSum = ...; if (spSum !== sq.maxScore) results.push({rule:'V8', level:'error', message:'Q'+(qi+1)+' 采分点分值之和 ('+spSum+') 不等于满分 ('+sq.maxScore+')'}); })`

### 第三批：复杂改动（2项，可选是否本轮做）

**P6. 验证结果可跳转**
- 验证规则（`englishEditValidateSave.js`）：每条 error 结果新增 `qi` 和 `spIndex` 字段
- 模板（第598-603行）：error 项变为可点击蓝色文字，点击后 `englishEdit.goToStep('per_question'); englishEdit.goToQuestion(vr.qi)`
- 较复杂，建议单独一轮

**P7. 脚本区域折叠**
- 模板（Step3 区域第533行）：脚本 textarea 包裹在 `el-collapse` 中，默认折叠
- 生成脚本后自动展开（computed 或 watch 控制展开状态）
- 自查/质量评估结果不受影响
- 建议单独一轮

## 关键文件

| 文件 | 用途 |
|------|------|
| `templates/question-bank.html` | 第180-634行英语编辑器区域 |
| `static/js/englishEditCore.js` | 状态和方法（JS 已改完） |
| `static/js/englishEditAI.js` | AI API（已改完） |
| `static/js/englishEditValidateSave.js` | V8 规则待加 |

## 验证方式

启动 `python main.py`，登录英语科目，打开英语题目逐一验证各项。
