# 英语编辑器体验优化 — 已完成

> 全部 7 项模板+验证规则优化已完成，11/11 测试通过。

## 已完成（JS 层，4f75aa4）

| # | 优化项 | 状态 |
|---|--------|------|
| 6 | AI 静默失败 → throw | ✅ |
| 9 | 采分点提取独立 loading | ✅ |
| 1 | 子题返回不重置后续题 | ✅ |
| 10 | 浮动点精度 | ✅ |
| 2 | 保存成功提示 | ✅ |
| 8 | confirm → ElMessageBox | ✅ |

## 已完成（模板 + 验证规则）

### 第一批：模板同步（2项）

| # | 优化项 | 改动 | 状态 |
|---|--------|------|------|
| P1 | 模板同步 `scoringPointExtractingMap` | `:loading` 绑定改为 `scoringPointExtractingMap[sq.tempId]` | ✅ |
| P2 | 模板同步 `confirmResetAll` | `@click` 改为 `englishEdit.confirmResetAll()` | ✅ |

### 第二批：新增功能（3项）

| # | 优化项 | 改动 | 状态 |
|---|--------|------|------|
| P3 | 步骤指示器锁定视觉提示 | 未解锁步骤 `opacity: 0.5` + `cursor: not-allowed`，点击提示"请先完成前面的步骤" | ✅ |
| P4 | 返回题库按钮（Step5） | 保存按钮旁新增"返回题库"按钮 | ✅ |
| P5 | 采分点分值小计 + V8 验证 | 采分点标题栏实时显示"分值合计: X / Y ✓/✗"，V8 规则确保每子题采分点之和 = 满分 | ✅ |

### 第三批：复杂改动（2项）

| # | 优化项 | 改动 | 状态 |
|---|--------|------|------|
| P6 | 验证结果可跳转 | error/warning 项变为可点击蓝色文字+下划线，点击跳转到对应子题；V2-V9 验证结果均含 `qi` 字段 | ✅ |
| P7 | 脚本区域折叠 | 脚本 textarea 用 `el-collapse` 包裹，默认展开，生成脚本后自动展开 | ✅ |

## 改动文件

| 文件 | 改动内容 |
|------|---------|
| `templates/question-bank.html` | P1-P7 模板改动（采分点 loading、重置按钮、步骤锁定、返回按钮、分值小计、验证跳转、脚本折叠） |
| `static/js/englishEditValidateSave.js` | V8 规则新增 + V2-V9 所有结果加 `qi` 字段 |
| `static/js/englishEditCore.js` | `scriptCollapseActive` 初始化 + 生成后自动展开 + 暴露到 return |
