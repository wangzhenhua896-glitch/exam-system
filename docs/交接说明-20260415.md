# 交接说明 2026-04-15

新 Agent 请先读这份文件，再开始工作。

---

## 一、今天完成的工作

1. **static/vendor/ 纳入版本管理**
   - 之前 vendor 在 .gitignore 中被排除，导致新机器 clone 后页面显示源码
   - 已从 CDN 下载 7 个前端库（Vue3/Element Plus/Axios 等），提交进 Gitea

2. **数据字典补全**（`docs/数据字典.md`）
   - 补全了 questions 表 3 个缺失字段：`content_html`、`question_type`、`workflow_status`

3. **英语 0007-0012 采分点写入数据库**
   - 为 ID 172-177（四大发明/书法/移动支付/丝绸之路/剪纸/北斗）各创建了 Q1/Q2/Q3 子题
   - 采分点 JSON 已写入 `question_answers` 表，子题 ID 为 216-233
   - 导入脚本：`scripts/import_english_scoring_points.py`
   - 已用 `/api/grade` 接口实测验证通过

4. **新增文档**
   - `docs/英语采分点批量录入分析.md` — 方案分析（为什么不用现有 AI 接口）
   - `docs/数据库版本管理说明.md` — 三个环境的数据库管理规范

---

## 二、待处理事项

- [ ] **阿里云服务器数据库未同步**：服务器（123.56.117.123）上还没有新采分点，如需服务器评分要手动同步数据库
- [ ] **ot_171.txt 临时文件**：根目录有个 `ot_171.txt`，是调试时留下的，可以删掉
- [ ] **验证评分效果**：对 172-177 每道题跑几个典型答案，确认评分结果符合预期

---

## 三、开始工作前必做

```bash
git pull   # 今天推了多个提交，必须先同步
```

---

## 四、相关文档索引

| 文档 | 说明 |
|------|------|
| `docs/英语采分点批量录入分析.md` | 英语采分点方案背景，为什么这样做 |
| `docs/数据库版本管理说明.md` | Gitea vs GitHub 区别，三个环境管理规范 |
| `docs/数据字典.md` | 数据库全部字段说明 |
| `scripts/import_english_scoring_points.py` | 英语采分点导入脚本（含全部12道题数据） |
