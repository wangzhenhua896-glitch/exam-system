# 英语题目编辑器 — 待办工作清单

> 更新日期: 2026-04-14
> 分支: `multi-fullscore-answers`
> 已完成提交: `a067a34`（5步工作流）→ `28738ac`（AI 建议自动填入）→ Bug 修复（未提交）

## 已完成（2 项）

- [x] **5 步工作流 UI 重构** — Step1~5 模板骨架 + 工作流状态机 + Tag 芯片交互 + 计分公式切换 + buildApiPayload 6 大约束
- [x] **AI 同义词/排除词建议自动填入** — el-popover 弹出建议面板，点击添加到 Tag 芯片，已存在词灰显禁用，支持"全部添加"
- [x] **后端测试** — 33/33 全部通过（DB 迁移、工作流状态、API 校验、buildApiPayload 格式、Prompt 导入、JSON 解析）
- [x] **数据库基础设施** — `question_type` / `workflow_status` 列 + CRUD 函数
- [x] **4 个 AI 接口** — `/api/english/extract`、`suggest-synonyms`、`suggest-exclude`、`generate-rubric`
- [x] **题型抽象层** — EDITOR_REGISTRY 注册表框架就位
- [x] **端到端测试 Bug 修复** — Bug 1（Q2/Q3 按钮 disabled）、Bug 3（textarea 累积）、Bug 4（进入下一步按钮不出现），详见 `docs/英语编辑器Bug修复记录-20260414.md`

---

## 待办事项

### P0 — 必须完成（阻塞发布）

#### 1. 端到端工作流测试（Step 1 → Step 5 完整走通）✅ 已完成一轮

**目标**：验证 5 步工作流的实际可用性，发现交互层面的 bug。

**已完成**：
- State A 测试：Q166（Chinese New Year），Playwright 自动化 + 手动观察
- 发现 4 个 Bug，其中 3 个已修复（Bug 1/3/4），1 个确认非 Bug（Bug 2）
- 修复记录：`docs/英语编辑器Bug修复记录-20260414.md`

**待回归验证**：
- 修复后需重新走一遍 Step 1 → Step 5，确认 Bug 1/3/4 不再复现
- 验证方法：启动服务 → 手动操作浏览器打开 Q166

---

#### 2. State B 加载验证（旧题 170-177）

**目标**：仅有 rubric_script、无子题/采分点的旧题目能正确进入编辑流程。

**当前状态**：`loadQuestion()` 有 State A/State B 分支逻辑，但从未在真实数据上验证。

**需验证**：
- 打开 Q170（High-Speed Rail），应显示引导卡片："此题目尚未配置结构化采分点"
- "从脚本提取"按钮 → 调用 AI 解析 rubric_script → 填充子题卡片 → 进入 Step 2
- "手动添加子题"按钮 → 手动创建子题 → 进入 Step 2

**测试数据**：Q170-Q177（8 道题均无结构化数据）

---

### P1 — 应完成（提升体验）

#### 3. 保存逻辑完整性验证

**目标**：`saveAll()` 函数的实际数据库写入正确性。

**需验证**：
- 父题 `content` 字段正确重建（阅读材料 + 子题题目 + 标准答案拼接）
- 子题 `standard_answer` 正确写入
- 子题 `rubric` JSON 格式正确
- `question_answers` 表 `scope_type='scoring_point'` 的 `answer_text` JSON 格式正确（与评分引擎消费格式一致）
- `workflow_status` 独立列正确更新
- 修改后重新加载能恢复编辑状态

**关键约束**：`buildApiPayload()` 输出的 JSON 必须与 `english_scoring_point_match()` 消费的格式完全一致（6 大格式约束）。

---

#### 4. 编辑已有题目加载验证

**目标**：已有结构化数据（Q166-Q169）的题目能正确加载并恢复编辑状态。

**需验证**：
- 子题列表正确加载
- 采分点关键词/同义词/排除词正确回填到 Tag 芯片
- `score_formula` 类型正确判断（max_hit_score 字符串 vs hit_count 对象）
- `scoring_rules` 正确回填（hit_count 型）
- `workflow_status` 恢复到上次保存的步骤进度

**测试数据**：Q166（排除词）、Q167（拼音白名单）、Q168/Q169（3 个采分点 hit_count）

---

#### 5. 实现记录文档更新

**目标**：将当前实现状态同步到 `docs/英语编辑器UI重构实现记录.md`。

**需更新**：
- 第十一节"待完成事项"中第 3 项"同义词/排除词自动填入"标记为已完成
- 第十二节对照表中 AI 同义词/排除词补全标记改为 ✅
- 补充 `28738ac` 提交的变更说明

---

### P2 — 可后续做（优化增强）

#### 6. 采分点提取专用端点

**现状**：`extractScoringPoints()` 复用 `/api/english/extract`，该端点设计为提取子题+采分点，单独提取采分点的效果可能不够精准。

**方案**：新增 `POST /api/english/extract-scoring-points`，Prompt 专注于从单个子题的标准答案中提取采分点，不涉及子题拆分。

**改动文件**：`app/api_routes.py`（新端点）、`app/english_prompts.py`（新 Prompt）、`static/js/englishEditCore.js`（调用端点）

---

#### 7. 质量评估集成

**现状**：`QUALITY_EVALUATION_SYSTEM_PROMPT_EN` 已存在但未接入工作流。

**方案**：在 Step 2 完成后或 Step 3 中，增加"命题质量评估"按钮，调用 AI 对题目质量打分。

**改动文件**：`templates/question-bank.html`（新增按钮+结果展示区）、`static/js/englishEditCore.js`（新增 `evaluateQuality()` 方法）

---

#### 8. 自查步骤集成

**现状**：`SELF_CHECK_RUBRIC_SYSTEM_PROMPT_EN` 已存在但未接入。

**方案**：在 Step 3（评分脚本生成）中，增加"自查"按钮，用 AI 对生成的评分脚本进行一致性自查。

**改动文件**：`templates/question-bank.html`、`static/js/englishEditCore.js`

---

#### 9. 同步到实现记录文档

**目标**：在本次完成后，同步更新 `docs/英语编辑器UI重构实现记录.md`。

**需更新**：
- 第十一节"待完成事项"中第 3 项"同义词/排除词自动填入"标记为已完成
- 第十二节对照表中 AI 同义词/排除词补全标记改为 ✅

---

### P3 — 长期（扩展性）

#### 10. 题型编辑器扩展

**目标**：EDITOR_REGISTRY 注册表已有框架，实现其他题型的编辑器。

| 题型 | 编辑器文件 | 预估行数 | 说明 |
|------|----------|---------|------|
| single_choice | `static/js/choiceEditorCore.js` | ~300 行 | 选项编辑器 + 正确选项选择 |
| multi_choice | 同上（复用） | ~200 行增量 | 加部分分配置 |
| fill_blank | `static/js/fillBlankEditorCore.js` | ~400 行 | 空位编辑器 + 各空答案 |
| true_false | 内嵌在 choiceEditor 中 | ~100 行 | 极简表单 |

**改动文件**：新增 JS 模块 + `question-bank.html` 注册 + 后端评分分支

---

#### 11. 测试依赖清理

**现状**：`test_workflow_status_requires_body` 依赖题目 ID=1 存在。

**方案**：改为 fixture，在测试开始前创建测试用题目，结束后清理。

**改动文件**：`tests/test_english_edit.py`

---

## 当前未提交的文件（与编辑器无关）

以下文件的修改不在编辑器范围内，需单独处理：

| 文件 | 说明 |
|------|------|
| `app/app.py` | Flask 配置变更 |
| `app/qwen_engine.py` | 评分引擎变更 |
| `deploy.sh` | 部署脚本变更 |
| `dist/index.html` | 单题评分页变更 |
| `templates/*.html` | 多个页面模板变更（非编辑器相关） |
| `static/js/use*.js` | 多个组合式模块变更 |
| `_tmp_test.js` | 临时测试文件，应清理或加入 .gitignore |
| `data/exam_system.db` | 数据库变更（需确认是否提交） |

建议检查这些文件是否应提交，或者是否是其他分支的修改混入。
